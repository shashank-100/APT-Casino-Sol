/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { ROULETTE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DECREMENT_DISCRIMINATOR = new Uint8Array([
  106, 227, 168, 59, 248, 27, 150, 101,
]);

export function getDecrementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DECREMENT_DISCRIMINATOR);
}

export type DecrementInstruction<
  TProgram extends string = typeof ROULETTE_PROGRAM_ADDRESS,
  TAccountRoulette extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountRoulette extends string
        ? WritableAccount<TAccountRoulette>
        : TAccountRoulette,
      ...TRemainingAccounts,
    ]
  >;

export type DecrementInstructionData = { discriminator: ReadonlyUint8Array };

export type DecrementInstructionDataArgs = {};

export function getDecrementInstructionDataEncoder(): Encoder<DecrementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DECREMENT_DISCRIMINATOR })
  );
}

export function getDecrementInstructionDataDecoder(): Decoder<DecrementInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDecrementInstructionDataCodec(): Codec<
  DecrementInstructionDataArgs,
  DecrementInstructionData
> {
  return combineCodec(
    getDecrementInstructionDataEncoder(),
    getDecrementInstructionDataDecoder()
  );
}

export type DecrementInput<TAccountRoulette extends string = string> = {
  roulette: Address<TAccountRoulette>;
};

export function getDecrementInstruction<
  TAccountRoulette extends string,
  TProgramAddress extends Address = typeof ROULETTE_PROGRAM_ADDRESS,
>(
  input: DecrementInput<TAccountRoulette>,
  config?: { programAddress?: TProgramAddress }
): DecrementInstruction<TProgramAddress, TAccountRoulette> {
  // Program address.
  const programAddress = config?.programAddress ?? ROULETTE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    roulette: { value: input.roulette ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.roulette)],
    programAddress,
    data: getDecrementInstructionDataEncoder().encode({}),
  } as DecrementInstruction<TProgramAddress, TAccountRoulette>;

  return instruction;
}

export type ParsedDecrementInstruction<
  TProgram extends string = typeof ROULETTE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    roulette: TAccountMetas[0];
  };
  data: DecrementInstructionData;
};

export function parseDecrementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedDecrementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      roulette: getNextAccount(),
    },
    data: getDecrementInstructionDataDecoder().decode(instruction.data),
  };
}
